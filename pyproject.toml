[project]
name = "imagedups"
version = "0.1.0"
description = "Locate image duplicates across file systems."
readme = "README.md"
requires-python = ">=3.10"

[project.scripts]
imagedups = "mymain:main"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
